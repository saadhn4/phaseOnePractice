// 3. Type Conversions
// - Convert the string `"123"` into a number and add 10 to it. Log the result and its type

let str = "123";
let result = Number(str);
let add = result + 10;
console.log(add, typeof add);
